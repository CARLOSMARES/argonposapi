# Nombre de usuario utilizado para autenticar la conexión a la base de datos.
DATABASE_USER

# Contraseña del usuario de la base de datos.
DATABASE_PASSWORD

# Dirección o dominio del servidor donde está alojada la base de datos.
# Ejemplo: localhost o una IP como 127.0.0.1, o un host remoto.
DATABASE_HOST

# Puerto de conexión a la base de datos.
# Ejemplo: 5432 para PostgreSQL, 3306 para MySQL.
DATABASE_PORT

# Nombre de la base de datos a la que se conectará la aplicación.
DATABASE_NAME

# Clave secreta usada para firmar y verificar los tokens JWT.
# Debe ser una cadena segura, preferentemente generada aleatoriamente.
JWT_SECRET

# Tiempo de expiración de los tokens JWT.
# Ejemplo: "1h" para una hora, "7d" para siete días.
JWT_EXPIRATION

# Servicio de correo que se usará. Ejemplo: 'gmail', 'sendgrid', 'outlook'.
EMAIL_SERVICE

# Dirección de correo electrónico del remitente.
EMAIL_USER

# Contraseña o token de aplicación del correo electrónico.
# En el caso de Gmail, se recomienda usar una "contraseña de aplicación".
EMAIL_PASSWORD

# Credenciales de PayPal para autenticación en su API.
PAYPAL_CLIENT_ID
PAYPAL_CLIENT_SECRET

# Credenciales de Stripe para autenticación en su API.
# La clave secreta se usa en el backend y debe mantenerse privada.
STRIPE_SECRET_KEY

# La clave publicable se usa en el frontend para interactuar con Stripe de forma segura.
STRIPE_PUBLISHABLE_KEY

# Credenciales de PayPal para autenticación en su API.
PAYPAL_CLIENT_ID
PAYPAL_CLIENT_SECRET

# Credenciales de Stripe para autenticación en su API.
# La clave secreta se usa en el backend y debe mantenerse privada.
STRIPE_SECRET_KEY

# La clave publicable se usa en el frontend para interactuar con Stripe de forma segura.
STRIPE_PUBLISHABLE_KEY